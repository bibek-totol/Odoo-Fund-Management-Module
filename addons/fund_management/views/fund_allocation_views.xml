<odoo>
    <record id="view_fund_allocation_tree" model="ir.ui.view">
        <field name="name">fund.allocation.tree</field>
        <field name="model">fund.allocation</field>
        <field name="arch" type="xml">
            <tree decoration-info="state in ('submitted','gm_approved')"
                  decoration-success="state == 'approved'"
                  decoration-danger="state == 'rejected'">
                <field name="name"/>
                <field name="fund_account_id"/>
                <field name="project_id"/>
                <field name="expense_head_id"/>
                <field name="amount"/>
                <field name="state"/>
                <field name="request_date"/>
            </tree>
        </field>
    </record>

    <record id="view_fund_allocation_form" model="ir.ui.view">
        <field name="name">fund.allocation.form</field>
        <field name="model">fund.allocation</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit" type="object" string="Submit"
                            class="btn-primary" states="draft"/>
                    <button name="action_gm_approve" type="object" string="GM Approve"
                            class="btn-primary" states="submitted"/>
                    <button name="action_md_approve" type="object" string="MD Approve"
                            class="btn-primary" states="gm_approved"/>
                    <button name="action_reject" type="object" string="Reject"
                            class="btn-danger" states="submitted,gm_approved"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            states="draft,submitted,gm_approved"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,submitted,gm_approved,approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="name" readonly="1"/>
                            <field name="fund_account_id"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="amount"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="request_date"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        </group>
                        <group>
                            <field name="project_id"
                                   attrs="{'readonly': [('state', '!=', 'draft')],
                                           'required': [('expense_head_id', '=', False)]}"/>
                            <field name="expense_head_id"
                                   attrs="{'readonly': [('state', '!=', 'draft')],
                                           'required': [('project_id', '=', False)]}"/>
                            <field name="requested_by" readonly="1"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                    <group>
                        <field name="purpose"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="attachment" filename="attachment_name"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="attachment_name" invisible="1"/>
                    </group>
                    <group string="Approval Comment" groups="fund_management.group_fund_manager">
                        <field name="approval_comment"/>
                    </group>
                    <notebook>
                        <page string="Approval History">
                            <field name="approval_history_ids">
                                <tree>
                                    <field name="approver"/>
                                    <field name="approval_level"/>
                                    <field name="result"/>
                                    <field name="date"/>
                                    <field name="comment"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_fund_allocation" model="ir.actions.act_window">
        <field name="name">Fund Allocations</field>
        <field name="res_model">fund.allocation</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>